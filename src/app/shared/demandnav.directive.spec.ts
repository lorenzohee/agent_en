import { DemandnavDirective } from './demandnav.directive';

describe('DemandnavDirective', () => {
  it('should create an instance', () => {
    const directive = new DemandnavDirective();
    expect(directive).toBeTruthy();
  });
});
